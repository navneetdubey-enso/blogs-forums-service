PORT=3000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/blogs_forums
REDIS_URL=redis://localhost:6379

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=blogs-enso
AWS_S3_SIGNED_URL_EXPIRES_IN=3600

# Public media URLs use virtual-hosted-style S3 links when visibility=PUBLIC.
# The bucket must allow public reads for those objects (ACL public-read and/or bucket policy).

